[project]
name = "seedream-imagegen-skill"
version = "1.2.0"
description = "Claude Code skill for validated Seedream image generation"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }

[tool.pytest.ini_options]
testpaths = ["tests"]
